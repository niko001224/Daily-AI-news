$(python3 -c "import json; data=json.load(open('/home/ubuntu/news_data.json')); 
report=['<span style=\"color:#1677ff;font-size:18px;font-weight:bold;\">AI 行业动态</span>'];
for cat in ['政策与监管', '模型与技术进展', '产品与应用发布', '投融资与并购', '行业落地与生态']:
    items=[i for i in data['ai_news'] if i['category']==cat]
    if items:
        report.append(f'\n**{cat}**')
        for i in items:
            report.append(f'\n{i["title"]}\n说明：{i["description"]}\n背景：{i["background"]}\n要点：')
            for p in i['points']: report.append(f'- {p}')
            report.append(f'来源出处：[{i["source"]}]({i["url"]})')
print('\n'.join(report))")
